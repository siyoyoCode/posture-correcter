# ── Install on the RASPBERRY PI ───────────────────────────────────────────────
#   pip install -r requirements_rpi.txt

requests
gpiozero
pyttsx3          # TTS fallback if espeak isn't available

# Camera (choose ONE — install via apt, not pip):
#   sudo apt install -y python3-picamera2    ← recommended (RPi OS Bullseye+)
#   pip install picamera                     ← legacy only

# Text-to-speech engine (recommended, install via apt):
#   sudo apt install -y espeak


# ── Install on your LAPTOP (only needed if using the relay server) ────────────
#   pip install flask flask-cors requests